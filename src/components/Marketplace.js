import React from 'react';

function Marketplace() {
    return (
        <div className="container">
            <h1>Marketplace</h1>
            <p>Explore the available carbon credits and find the right ones for your needs.</p>
            {/* This would eventually list out available carbon credits, probably using a component for each credit */}
        </div>
    );
}

export default Marketplace;
